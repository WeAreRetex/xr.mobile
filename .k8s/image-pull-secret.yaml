apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: xr-bo-mobile
type: kubernetes.io/dockerconfigjson
data:
  # Questo è un placeholder. Dovrai generare questo valore con:
  # kubectl create secret docker-registry ghcr-secret --docker-server=ghcr.io --docker-username=OWNER --docker-password=YOUR_PAT --docker-email=YOUR_EMAIL -n xr-bo-mobile --dry-run=client -o yaml
  # e poi copiare la sezione .dockerconfigjson
  .dockerconfigjson: BASE64_ENCODED_DOCKER_CONFIG
